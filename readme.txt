Basic functionalities::
    1. System allows user to upload the data and it stores it in the system.
    2. system processes the data and it gives basic insights. As per now, insights are already defined into the system.
    3. Also as per now, the system allows to upload data which is already set up into the system, it doesnt allow to upload any other kind of data.
    4.if the user uploads the data which is already saved into the system, the system will update the existing records and create new ones.
    5. Data analytics change when the data is added or removed